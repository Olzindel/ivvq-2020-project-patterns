type Product {
    id: ID!,
    name: String!,
    price: Float!,
    status: String!,
    createdAt: String,
    imageLinks: [ImageLink!]!,
    merchant: Merchant!
}


extend type Query {
    products(count: Int!):[Product]
    product(productId: ID!):Product
}

extend type Mutation {
    createProduct(name:String!, price:Float!, status:String!, description:String, imageLinkIds:[ID!], merchantId:ID!):Product
    deleteProduct(productId:ID!):ID
}