type OrderItem {
    id: ID!,
    quantity: Int!,
    product: Product!,
    order: Order!
}


extend type Query {
    orderItems(count: Int!):[OrderItem]
    orderItem(orderItemId: ID!):OrderItem
}

extend type Mutation {
    createOrderItem(quantity:Int!, productId:ID!, orderId:ID!):OrderItem
    deleteOrderItem(orderId:ID!):ID
}